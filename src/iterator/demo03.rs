/*
消费迭代器的方法

| 方法          | 描述                   |
| ----------- | -------------------- |
| `collect()` | 收集为容器（如 Vec、HashSet） |
| `sum()`     | 求和                   |
| `product()` | 求积                   |
| `all()`     | 所有元素满足条件             |
| `any()`     | 任意元素满足条件             |
| `find()`    | 查找第一个满足条件的元素         |
| `fold()`    | 手动聚合，类似 reduce       |

 */

fn main() {

}